# DX 파이프라인 + MoA orchestrator 필수 라이브러리 (v2.4 — 재현성 위해 버전 범위 고정)
# 설치: pip install -r requirements.txt
pandas>=2.0,<3.0
numpy>=1.24,<3.0
scikit-learn>=1.3,<1.7
scipy>=1.10,<2.0
matplotlib>=3.7,<4.0

# 1단계: 데이터 수집
google-play-scraper>=1.2

# 2단계: 한국어 형태소 분석 (Java 불필요, pip만으로 설치됨)
kiwipiepy>=0.17

# 3단계: 딥러닝 임베딩 (Ko-SBERT) — 선택 (없으면 TF-IDF만 사용)
sentence-transformers>=2.2,<4.0

# 5단계: 감성 분석 + 토픽 모델링 — 선택 (없으면 평점/LDA 폴백)
transformers>=4.35,<5.0
torch>=2.0
# bertopic은 hdbscan/umap 컴파일 의존성으로 Windows에서 설치 실패가 잦음.
# 실패해도 LDA 폴백으로 동작하므로 설치 실패 시 주석 처리하고 진행 가능.
bertopic>=0.16

# MoA orchestrator (moa_orchestrator.py) — GPT 서브 에이전트 호출
openai>=1.0,<3.0

# 정확한 재현이 필요하면: pip freeze > requirements-lock.txt
