# 인계 노트 — 다음 창으로 (2026-07-29)

이 문서는 **새 세션이 콜드 스타트로 이어받기** 위한 인계 노트다.
전판: [HANDOFF_2026-07-28.md](HANDOFF_2026-07-28.md) (Epic 1~4·UX·설문 v4 시점 — 계보용 보존).

> **먼저**: 반드시 `cd C:\Users\user\Desktop\DX_project ; claude` 로 열 것.
> **새 코드/산출물 만들기 전에 [REVIEW_PLAYBOOK.md](REVIEW_PLAYBOOK.md)를 읽을 것.**
> 복명복창: 매 턴 앞에 주제("집이 나를 따라온다")·프로젝트 정체·현재 단계를 밝힌다.

## 오늘(07-29) 한 일 — 전부 완료·푸시됨

| 트랙 | 상태 |
|---|---|
| **MoA v3.0 체제 전환** | 자기 보고 신뢰도 폐지(3회 날조) → **G 감사 점수** 직접 산출. [G_AUDIT_SCORE.md](G_AUDIT_SCORE.md) v1.0 daria 승인(건수 비례·VOC 접속 유지·v4부터 적용). CLAUDE.md v2.9. E 오염 경로 차단(A키 개명+금지 범위 출력 전체 — Winston 안). 5회전 전 항목 청정, **첫 종합점수 97.6점** |
| **페르소나** | Quietly Home **T2 정식 등재**(PERSONA_LADDER). E는 Night Keeper/Quietly Home 랜덤 배정, `PERSONA_FORCE` 환경변수로 강제 가능 |
| **Epic 5 Docker 실증** | ✅ 완료 — 6/6 테이블 적재(reviews 12,585 등), queries.sql Q1~Q5 실증. 좀비 소켓 부검: 치료 = **재부팅+run 폴더 재격리 조합** (deferred-work §Epic 5). 테스트 **426 passed** |
| **CX 보고서 v4** | [CX_분석결과.md](final-output/CX_분석결과.md) — 감사 점수 체제 반영 |
| **사업화 문서 11종+INDEX** | [planning-artifacts/biz-docs/](planning-artifacts/biz-docs/00_INDEX.md) — 조달 수명주기 4단계, 라벨 3종(`[실측]`/`[공개출처]`/`[가정]`), R-5에 3회 날조 적발을 리스크 실증 사례로 등재 (포폴용) |
| **설문 실사·정정** | 아래 ⚠️ 참조 — 기록 오기 정정 완료 |

커밋: `7d77492`(MoA v3.0) → `4b41f98`(Epic 5) → `95c8cfe`(biz-docs+설문 정정).
옵시디언 미러: `ob_storage/DX_project/` — CX v4·사다리·감사점수·사업화문서 12종·작업로그.

## ⚠️ 설문 — 실제 상태 (07-28 기록은 오기였음)

- **본확산 미시행** — 채널 게시는 아직 안 했다. 확산 패키지(채널별 문구+프리필
  링크)는 [SURVEY_DEPLOY.md](SURVEY_DEPLOY.md) §v4에 준비돼 있음 — 게시만 하면 됨
- 현재 응답 **4건**(유효 2 — 1인가구·기타, 스크리닝 탈락 2), 채널 유입 0(전원 지인)
- **파일럿 3건(G1 유자녀1+G2 신혼2) 소실 — 원인 미상.** 사본 폼 3개 전수 조사로
  사본 오응답 가설은 기각(전부 0건). **daria가 파일럿 3명에게 제출 완료 화면
  봤는지 확인 중** ← 다음 세션에서 결과 받을 것
- 동명 사본 폼 3개는 "[구버전-배포금지]"로 개명 격리 — 본 폼(`1x-JqmEH…`)만 유효

## 다음 창에서 할 일 (우선순위 순)

1. **파일럿 확인 결과 반영** — 미제출이었으면 재응답 요청, 제출이었으면 삭제
   경위 조사(응답 시트 연동 후 재발 방지). SURVEY_DEPLOY §배포 이력에 기록
2. **채널 본확산 게시** (daria 수동) — 게시 후 SURVEY_DEPLOY 이력에 채널·시각 기입
3. **응답 추이 관찰** → 마감 시: CSV → `survey_raw.csv` → survey_encode →
   04_segmentation → PERSONA_LADDER §5 승격/기각 → 발표 라벨 일괄 갱신
4. **발표 서사 프레임 확정** (팀 결정 대기) — "산출 불가→감사점수" 전환을
   "1회 사고"가 아닌 "모델 특성→게이트 상시 작동"으로 서사화할지 (CX v4 결정 A)
5. 여유 시: GPT 이미지 2번 재생성분 검수(24° 부부 축·체크 제거·라운드 워치)

## 운영 메모 (오늘 추가분)

- **MoA 실행**: `python moa_orchestrator.py` — E 페르소나 랜덤 배정 로그 확인.
  강제: PowerShell에서 `$env:PERSONA_FORCE="Quietly Home"` (또는 "Night Keeper")
- **G 검수**: 신뢰도는 G_AUDIT_SCORE.md v1.0 공식으로 직접 채점 — 서브 출력에
  신뢰도 수치가 보이면 그 자체가 규칙 위반(오염 의심 체크리스트행)
- **Docker DB**: 기동 중(onme_discovery_db, 포트 5433). 비밀번호는
  `%LOCALAPPDATA%\Temp\onme_db_pw.txt`(로컬 전용, 저장소 미기록). 원복은
  `docker compose -f db/compose.yml down -v`. Docker Desktop 재기동 실패 시
  deferred-work §Epic 5 부검 참조(재부팅+run 격리)
- 격리 폴더 3개(`Docker/run_stale*`·`docker-secrets-engine_stale`)는 삭제 불가
  방치 확정(무해)
- 파티 메모리 10엔트리 — 오늘 평결 2건(감사점수·사업화문서) 기록됨
