# 인계 노트 — 다음 창으로 (2026-07-23)

이 문서는 **새 세션이 콜드 스타트로 이어받기** 위한 인계 노트다.
오늘(2026-07-22~23) Epic 1·2를 완주했고, 아래 3건이 다음 창 몫이다.

## 지금까지 (완료)

- **Epic 1 완료**: 홈 프로필 스키마·저장·캐리어 중립(1.1~1.3)
- **Epic 2 완료**: 가전 시뮬레이터·프로필 명령전송·오프라인 강제·야간 시나리오(2.1~2.4)
- 테스트 **296 passed**, 최신 커밋 `acdb984` 푸시 완료
- **PostgreSQL 도입 결정**: 발견 트랙 전용, Epic 5, Docker. [meetings/2026-07-22_postgres-portfolio-track.md]
- **LGE 키워드 매트릭스**: [LGE_KEYWORD_MATRIX.md] — 업계 jargon 기준선(공식 홈 크롤링)

## ⚠️ 다음 창에서 할 일 (우선순위 순)

### 1. 흐름 차트 / API 설계 흐름도 (.md) — 파티가 약속함
코드 연관성·층 구조를 보여주는 흐름도를 `.md`로. 파티 검수(2026-07-22)에서 나온
요구사항 그대로:
- **층 구조**: `home_profile`(제품 코어) → 명령 bytes → `appliance_sim`(시뮬레이터)
- **역류 1건 빨간 화살표로 표시**: `appliance_sim/transports/loopback.py:53`이
  `home_profile.routine.reassemble`를 import한다(R5 이월 — 청킹이 잘못된 집).
  Epic 3에서 저장 매체 바뀔 때 올바른 위치가 드러남
- **힘 줄 곳 / 뺄 곳**을 시각적으로:
  - 🔴 **힘 줄 곳 = P-2 이동성(Epic 3)** — 유일한 실증 차별화(경쟁 2.0배 열위)인데
    코드·데모 0. 발표 서사가 P-1(오프라인)에 과집중. **P-2가 진짜 클라이맥스여야** 함.
    LG 언어로는 "삼성은 지키는 **고객감동**을 LG는 못 지키는 지점"([LGE_KEYWORD_MATRIX §3])
  - ⚪ **뺄 곳 = `ble_bless.py`** — 아래 3번 결정 대기

### 2. 2.4 코드 정리 (경미)
- `demo_night.py:122` — `run = execute_routine` 죽은 별칭. `execute_routine` 직접 호출로.
  (파티 2.4 리뷰 유일 지적, 경미)

### 3. `ble_bless.py` 존치 결정 → DECISIONS.md
Epic 2 완료로 근거 다 모임:
- 2.1: bless가 이 환경에서 **설치 불가**(상류 패키징 자기모순, 실측)
- 2.3: 실기기 데모에서 **가민 워치가 Central 역할** → Python 주변장치 경로 불필요
- 결정: 삭제 vs 미구현 표기 유지. 사람 판단 필요 → `docs/DECISIONS.md`에 올릴 것

## 확정된 서사 프레임 (다음 창이 반드시 유지)

- **주제**: "집이 나를 따라온다(Home Follows You)" / 프로토 **ThinQ OnMe**
- **제품명 방향 (daria 확정 2026-07-23)**: ThinQ OnMe ≈ LG 공식 **ThinQ ON**은
  충돌이 아니라 **결에 맞는 강점**. "ThinQ ON을 몸에 새기다 — ThinQ OnMe."
  LG 공식 방향의 **정당한 계승이자 미완성(P-2)의 완성**. 이름 자체가 P-2를 반박.
- **톤 규칙**: LG를 공격하지 말고 **LG 언어로 LG의 미완성을 잇는다**(정도경영·고객감동·
  Effortless·Affectionate Intelligence). painpoint를 LG 자기약속 위반으로 재서술.
- **jargon**: P-1은 **SPOF**(Single Point of Failure)로 부른다(대중어 "서버 없이" 대신).
  robot_cleaner·refrigerator는 jargon 아님(제품 카테고리).
- **정직 표기 서명 유지**: 시뮬레이터·미측정·미검증·"산출 불가". 1.1~2.4 관통.

## ⚠️ bmad config 주의 (변함없음)
`_bmad/bmm/config.yaml`은 Desktop 전역이라 crm-targeting-lab 창과 서로 덮어쓴다.
오늘 하루 **18번+** 뒤집혔다. bmad 스킬 실행 **직전에** 아래 3줄이 DX_project를
가리키는지 눈으로 확인하고, 명시 경로(`--story <path>`)로 작업해 의존을 줄일 것:
```yaml
planning_artifacts:       "{project-root}/DX_project/docs/planning-artifacts"
implementation_artifacts: "{project-root}/DX_project/docs/implementation-artifacts"
project_knowledge:        "{project-root}/DX_project/docs"
```

## 문제정의 기본 순서 (숙지 — daria 지시 2026-07-23)
문제정의·네이밍·톤 검수 때마다: **대상 기업 공식 홈 크롤링(비전·연혁·슬로건) →
최빈 키워드 → 키워드 매트릭스 → 우리 용어·톤을 그 언어에 맞춘다.** 제품 카테고리
이름은 jargon이 아니다. (메모리 [[naming-jargon-rule]] · [[lge-keyword-matrix]])
