# 인계 노트 — 다음 창으로 (2026-08-06)

이 문서는 **새 세션이 콜드 스타트로 이어받기** 위한 인계 노트다.
전판: [HANDOFF_2026-07-31.md](HANDOFF_2026-07-31.md) (성숙도 라벨·확산 패키지 점검 시점 — 계보용 보존).

> **먼저**: 반드시 `cd C:\Users\user\Desktop\DX_project ; claude` 로 열 것.
> **새 코드/산출물 만들기 전에 [REVIEW_PLAYBOOK.md](REVIEW_PLAYBOOK.md)를 읽을 것.**
> 복명복창: 매 턴 앞에 주제("집이 나를 따라온다")·프로젝트 정체·현재 단계를 밝힌다.

---

## 🚨 오늘의 사건 — 설문 트랙 종료 (NDA)

**전판(07-31)의 다음 창 1순위였던 "파일럿 확인 → 채널 본확산"은 전부 무효다.**
그 계획을 이어받지 마라.

2026-08-06, **NDA로 설문 응답 수집·활용이 금지**됐음이 확인됐다(daria).
채널 게시 직전이었고, 게시 전이었던 것이 다행이다.

- 단일 출처: **[SURVEY_TRACK_CLOSURE.md](SURVEY_TRACK_CLOSURE.md)**
- 대안 **A+B 채택**(daria 승인), C는 기각
- **VOC 74,434건 계보는 무관·유효** — 공개 플레이스토어 리뷰 직접 수집분이지
  LG가 제공한 사내 고객 데이터가 아니다. Pain Point·CX 보고서·PoC·사업화 문서 전부 생존
- **TRL 판정(PoC / TRL 3)도 불변** — 설문은 계측기이지 제품 축이 아니다

### 죽은 사슬

```
설문 → 04_segmentation → seg_manifest(source_type=survey)
     → ThinQ Village 합성패널(실모드) → PERSONA_LADDER §5 승격/기각
```

`synthetic_panel.py` L112~120의 `survey.csv` 실해시 대조 게이트가 구조적으로 막는다.
**우회하지 않는다** — 게이트가 제 일을 한 것이다.

### 관통 원칙 — 다음 세션이 반드시 지킬 것

> **T2를 기각하지 않았다. "가설 — 검증 미실행(NDA)"이다.**
> 기각도 검증의 결과이므로, 검증하지 않은 것을 기각이라 쓰면 그 자체가 또 다른 날조다.
> 구 표기 "가설 — 설문 검증 대기"는 **대기가 아니라 종료**이므로 문구를 교체한다.

---

## 오늘(08-06) 한 일

| 트랙 | 상태 |
|---|---|
| **설문 트랙 종료 문서화** | [SURVEY_TRACK_CLOSURE.md](SURVEY_TRACK_CLOSURE.md) v1.0 신설 — 설문 관련 주장·계획의 단일 출처. 발표 워딩(쓸 것 3·금지 4) 포함 |
| **포인터 일괄 삽입 8곳** | README·SURVEY_PLAN·SURVEY_DEPLOY·PERSONA_LADDER·WORKFLOW §4·MATURITY_POSITION·CX_DEFINITION·DEMO_SCRIPT. CX 보고서 v4.1(결정 B 종결), G_AUDIT_SCORE(T2 라벨 문구 교체·소급 감점 없음) |
| **H3 VOC 대체 가능성 실측** | ❌ 불가 확인 — reviews_clean 12,585건 중 혼수 4·입주 2·맞벌이 3건. 표본 자체가 없다 |
| **B 공개 2차 자료 조사** | [PUBLIC_STATS_B.md](PUBLIC_STATS_B.md) v1.0 — 리서치 5 + 수치별 적대 검증 23, 총 28 에이전트. 채택 다수 / **제외 1·단서 2** |

커밋: `67f5ded`(트랙 종료) → `e70276c`(2차 자료). 전부 푸시·미러됨.

### B 조사 채택 수치 `[공개출처]`

| 항목 | 수치 | 출처·기준 |
|---|---|---|
| 1·2인 가구 합계 | **66.1%** (1,487만) | 2025 인구주택총조사(발표 2026-07-28) |
| 혼인 건수 | **24만 건**/년 (+8.1%, 2년 연속 증가) | 2025 혼인·이혼 통계 |
| **30대 맞벌이** | **63.3%** (유배우 전체 48.6%) | 2025 지역별고용조사 — T2 모수로 가장 직접적 |
| 혼수 가전 | 1,445만원 / 냉장고 85.9% | 듀오 2026 ⚠️ **민간 조사** |

**⚠️ 인용 전 [PUBLIC_STATS_B §6 발표 인용 규칙](PUBLIC_STATS_B.md)을 읽을 것.** 특히:

- **스마트홈 보급률 51.3%는 제외된 수치다** — 쓰지 마라(기준 연도 미상 + 원출처 미확인
  + 갤럽 실측 8%와 6배 차이)
- **스마트워치는 12.9%(방통위·보유)와 33%(갤럽·이용)를 반드시 병기** — 33% 단독 인용은 부풀리기
- 1인 가구 36.6%의 **분모는 총가구가 아니라 일반가구**
- 정책브리핑 브리핑 전문이 1·2인 합계를 "1인 가구"로 **오기**했다 — 그대로 인용하면 모순

---

## 🔥 daria 확인 대기 (3건)

| # | 항목 | 성격 |
|---|---|---|
| 1 | **기수집 응답 4건 폐기 여부** | NDA 문언 확인 필요 — **G 판단 범위 밖** |
| 2 | **구글 폼 응답 수신 중지** | 폼 편집 화면 > 응답 탭 > "응답받기" 토글 (Apps Script 아님) |
| 3 | **파일럿 3명 접촉 중단** | 제출 여부 확인을 진행 중이었으나 중단 |

> **파일럿 3건 소실 원인은 미규명인 채로 종결됐다.** 사본 오응답(07-29)·수신 중지(07-31)
> 두 가설만 기각됐고, 남은 가설(폼 UI 삭제 / 미제출)은 응답자 접촉 없이는 확인 불가라
> 트랙 종료와 함께 닫았다. 이 미해결을 숨기지 않고 기록한다.

---

## 다음 창에서 할 일 (우선순위 순)

1. **daria 확인 3건 반영** — 특히 응답 4건 처리 결과를
   [SURVEY_TRACK_CLOSURE §5](SURVEY_TRACK_CLOSURE.md)에 기록
2. **발표 서사 재구성** — 타깃 검증 축이 비었으므로 서사 한 줄이 바뀐다:
   `발견(공개 리뷰 74,434건) → 정의(Pain·T1) → PoC/TRL 3~4 → [비어 있음] 타깃 검증(NDA) → 워치 앱 Prototype`
   비어 있음을 **먼저 밝히는** 형태로 장표 구성
3. **TRL 노출 수위 결정** (팀 결정 대기) — [MATURITY_POSITION §8](MATURITY_POSITION.md) 후속조치 1
4. **장표 실물 반영** — 성숙도 라벨 + NDA 공백 둘 다. 저장소 밖이라 daria 직접
5. **발표 서사 프레임 확정** (팀 결정 대기) — "산출 불가→감사점수" 전환을
   "모델 특성→게이트 상시 작동"으로 서사화할지 (CX v4 결정 A)
6. 여유 시: GPT 이미지 2번 재생성분 검수(24° 부부 축·체크 제거·라운드 워치)

> **하지 말 것**: 설문 재개·응답 분석·합성패널 실모드 시도. `--demo`(워터마크 유지)는
> 파이프라인 검증 목적으로만 허용하고 발표에 쓰지 않는다.

---

## 운영 메모

### 오늘 추가분

- **2차 자료 채택 기준**(DECISIONS 2026-08-06): ①기준 연도 미상 ②원출처 미확인
  (인용의 인용) ③기존 실측과 수배 차이 — 하나라도 걸리면 **제외**. 출처 간 불일치는
  유리한 쪽 선택 금지·**병기 강제**. 민간 조사는 표본 편향 단서 필수
- **에이전트 검증 결과를 그대로 믿지 말 것.** B 조사에서 검증자 23건 전원 confirmed였으나,
  그 confirmed는 *"그 문서에 그 수치가 있다"* 까지다. **통계 자체의 신뢰성은 G가 따로
  판단해야 한다** — 실제로 1건 제외·2건 단서 부착이 그 판단에서 나왔다
- **공개 데이터 vs 사내 데이터 구분을 먼저 하라.** NDA 상황에서 가장 먼저 확인한 것이
  "VOC가 어디서 왔는가"였고, 공개 크롤링임이 확인되면서 프로젝트 주력이 살았다
  ([crawl_playstore.py:25](../dx_pipeline_v2.2/crawl_playstore.py) `com.lgeha.nuts`)

### 이전 창에서 이어짐

- **Apps Script 함수 드롭다운 확인 필수** — 기본값이 첫 함수라 그대로 누르면
  `createSurveyForm`이 돈다. 이제 `guardAgainstDuplicate_()`가 막지만 로그를 보고 놀라지 말 것
- **편집기 코드는 저장소본이 진실** — `cat docs/survey_form_V4.gs | clip`으로 통째 교체.
  부분 붙여넣기는 `LIVE_FORM_ID` 미정의로 전 함수가 터진다
- **응답 원문 보존 원칙**(daria 07-31): 답변·인용은 가공하지 않고 원문 그대로.
  ※ 설문 응답에는 더 이상 적용 대상이 없으나, **VOC 리뷰 인용에는 그대로 유효**
- **옵시디언 미러는 잘 낡는다.** 원본 대조 없이 신뢰하지 말 것. 헤더(frontmatter +
  `> [!note] 저장소 미러`) 아래 원본 본문을 그대로 싣는 구조라, 헤더만 보존하고
  본문을 갈아끼우면 재생성된다
- **MoA 실행**: `python moa_orchestrator.py` — 강제 배정은
  `$env:PERSONA_FORCE="Quietly Home"` (또는 "Night Keeper")
- **G 검수**: 신뢰도는 [G_AUDIT_SCORE.md](G_AUDIT_SCORE.md) v1.0으로 직접 채점.
  서브 출력에 신뢰도 수치가 보이면 그 자체가 규칙 위반
- **Docker DB**: onme_discovery_db 포트 5433. 비밀번호
  `%LOCALAPPDATA%\Temp\onme_db_pw.txt`(로컬 전용). 원복 `docker compose -f db/compose.yml down -v`.
  재기동 실패 시 deferred-work §Epic 5 부검 참조(재부팅+run 격리)
- 격리 폴더 3개(`Docker/run_stale*`·`docker-secrets-engine_stale`)는 방치 확정(무해)
- 동명 사본 폼 3개는 `[구버전-배포금지]` 개명 격리 — 본 폼(`1x-JqmEH…`)만 유효.
  **폼은 삭제하지 않는다** — 문항 설계는 그 자체로 산출물

---

## 정정·미해결 (숨기지 않는다)

운영 원칙 4에 따라 남긴다.

- **파일럿 3건 소실 — 원인 미규명 종결.** 두 가설만 기각, 나머지는 확인 경로 상실
- **폼 제목 "3분→5분" 판단 오류(G, 07-31)**: 스크립트에 `setTitle`이 있다고 라이브에
  반영된 줄 알았으나 실측은 달랐다. **스크립트만 보고 상태를 단정하지 말 것**
- **스마트홈 보급률 51.3% 제외(G, 08-06)**: 채택했으면 발표에서 반박당했을 수치.
  검증 파이프라인이 아니라 **G의 사후 판단**이 걸러냈다는 점이 남을 만하다
