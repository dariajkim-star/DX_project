# GPT 교차 리뷰 프롬프트 — demo_quietly.py (2026-07-28)

> 서브에이전트 월 한도로 정식 3자 리뷰 불가 → PLAYBOOK §1 폴백: 정직성 렌즈를
> GPT 교차로. **아래 전문을 GPT에 붙여넣고, demo_quietly.py + tests/test_quietly_home.py
> 파일 내용을 함께 첨부할 것.** 결과는 이 파일 하단에 붙여 커밋.

---

당신은 냉소적 코드 리뷰어다. 첨부한 Python 데모(`demo_quietly.py`)를 리뷰하라.
이 데모의 주장: "부부 각자의 홈 프로필 설정이 이사(다른 기기 집합)를 건너
각자의 값으로 생존한다. 실행은 원터치이며 감지·서버·상태공유가 없다."

특히 다음을 사냥하라:
1. **거짓 출력** — 화면의 어떤 문장/판정이 실측 없이 합성되는가?
   (판정 기호 ✓✗가 상수이거나, 측정 안 한 값을 측정한 척하는 곳)
2. **실패 경로의 거짓** — serialize/map/execute가 실패할 때 rc와 화면이
   진실한가? 부분 실패(한 사람만 매핑 실패 등)가 성공처럼 보이는 경로는?
3. **주장 과잉** — "재설정 0회"·"각자의 값 그대로"가 코드가 실제 증명하는
   범위를 넘는가? (예: 먼저 잔 사람 프로필은 실행 없이 표시만 된다)
4. **경계** — 두 프로필이 같은 old_ref 문자열을 쓰는 것의 함의, 항등식 계산이
   map_to_new_home 리포트 계약과 어긋날 수 있는 입력.

출력: [심각도 High/Med/Low] 위치 / 결함 한 문장 / 구체적 실패 시나리오.
발견 없으면 '없음'. 칭찬 금지.

---

## GPT 리뷰 결과 (daria가 붙여넣기)

(대기)

(2026-07-28 접수 — daria 경유. GPT는 파일 미첨부 상태에서 리뷰를 거부하고 문서
결함 3건만 보고 → 브리프 수정 커밋 a5e682b. 파일 첨부 후 코드 리뷰 6건 접수.)

## 판정 분류 (step-04)

| # | 발견 | 판정 | 처리 |
|---|---|---|---|
| F1 High | blocked 표기에도 rc 0 + 성공 결론 | patch | `_one_touch` all_ok 전파, 불일치 시 rc 1 + 성공 문구 미출력. 회귀 테스트(noop 실행기 주입) 추가 |
| F2 High | 이사가 메모리 원본 우회 — "손목을 따라온다" 미증명 | patch(강화) | 워치=청크 레이아웃(persist_to_carrier), **이사·실행 전부 restore_from_carrier 경유**로 재배선 |
| F3 High | 먼저 잔 사람은 표시만 — 거짓 증명, 테스트가 문자열만 고정 | patch | 장면 4에서 **두 사람 다** 각자 새 워치 persist + 원터치 실행 실측(26·24). 테스트를 실행 실측 단언으로 교체 |
| F4 Med | 조명 판정이 우연한 최종 상태 | patch | 키 **존재**(명령 적용 증거) + 값 대조로 판정 |
| F5 Med | 감지 금지 테스트가 긍정형 감지 통과 | patch | 정확한 부정 문장 존재 + 금지 문구("감지 후"·"감지 완료"·"자동 감지"·"감지되") 부재 단언 |
| F6 Med | "이탈의 순간이 되지 않는다" 과잉 결론 | patch | 결론을 입증 범위로 축소("지원되는 설정은 재설정 없이…보류는 사유와 함께") + 이탈 방지=설계 의도(가설)·미측정 라벨 |

전 건 patch — intent_gap/bad_spec 없음. 반영 후 419 passed (회귀 0).
