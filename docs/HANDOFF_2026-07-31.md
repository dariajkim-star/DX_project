# 인계 노트 — 다음 창으로 (2026-07-31)

이 문서는 **새 세션이 콜드 스타트로 이어받기** 위한 인계 노트다.
전판: [HANDOFF_2026-07-29.md](HANDOFF_2026-07-29.md) (MoA v3.0·Epic 5 실증 시점 — 계보용 보존).

> **먼저**: 반드시 `cd C:\Users\user\Desktop\DX_project ; claude` 로 열 것.
> **새 코드/산출물 만들기 전에 [REVIEW_PLAYBOOK.md](REVIEW_PLAYBOOK.md)를 읽을 것.**
> 복명복창: 매 턴 앞에 주제("집이 나를 따라온다")·프로젝트 정체·현재 단계를 밝힌다.

## 오늘(07-31) 한 일

| 트랙 | 상태 |
|---|---|
| **성숙도 라벨 확정** | [MATURITY_POSITION.md](MATURITY_POSITION.md) v1.0 신설 — 단계 라벨의 **단일 출처**. 통합 판정 **PoC / TRL 3**(최저 구성요소 BLE TRL 2·워치앱 TRL 3), 검증된 프로필 코어 단독 TRL 4, 분석 도구 TRL 5~6. 발표 금지 표현 4종 확정 |
| **문서 용어 통일** | "프로토 제품명"→**PoC 제품명**, "프로토타입"→**시뮬레이터 PoC**. README·CX_DEFINITION·LGE_KEYWORD_MATRIX·PROTO_KICKOFF·biz-docs 8종·ux-designs 2종. DEMO_SCRIPT §0 성숙도 선언 신설 |
| **옵시디언 미러 대동기화** | 미러 **17개가 원본보다 낡아 있던 것**을 발견·전량 재생성. 특히 설문설계서 미러가 v3.1에 멈춰 있어 옵시디언에서만 읽으면 H3 가설이 안 보이는 상태였다 |
| **확산 패키지 점검** | 게시 불가 판정 → 차단 1·수정 4 반영. **게시 전 체크리스트 7항** 신설 |
| **폼 안내문·제목 정정** | `fixNoticeV5()` 적용 → `verifyNoticeV5()` **PASS 10 / FAIL 0** |
| **중복 생성 가드** | `createSurveyForm`에 `guardAgainstDuplicate_()` — 사본 폼 오생성 구조적 차단 |

커밋: `bede3cc`(성숙도) → `67a3487`(확산 점검) → `a40c4fe`(1차 실측) → `58865f7`(가드)
→ `7f543a0`(ID 상수화) → `e87bde8`(FAIL 0) → `19ceec1`(프리필 검증). 전부 푸시됨.

## 🔥 지금 대기 중 — 파일럿 3명 답변

**daria가 파일럿 3명에게 제출 여부를 묻는 중이다. 이 답이 다음 세션의 시작점이다.**

판정 분기:

- **"제출 화면 못 봤다"(미제출)** → 원인 종결. 체크리스트 1번 닫고 **바로 채널 게시**로 간다
- **"제출했다"** → 더 심각하다. 제출된 응답이 폼에서 사라졌다는 뜻이므로
  본확산 전에 원인을 더 파야 한다. 대량 게시하면 같은 방식으로 날아간다

소실 가설 소거 현황 — **검증 가능했던 것은 다 닫혔다**:

| 가설 | 결과 |
|---|---|
| 사본 폼 오응답 | ❌ 기각 (07-29, 사본 3개 전부 응답 0건) |
| 폼 응답 수신 중지 | ❌ 기각 (07-31, `verifyNoticeV5` `응답 수신 중` OK) |
| 폼 UI에서 응답 삭제 | 미검증 — 로그로 확인 불가 |
| 파일럿 미제출 | 미검증 — **파일럿 본인 확인만이 유일한 경로** |

## 설문 — 게시 전 체크리스트 현황

[SURVEY_DEPLOY.md §게시 전 체크리스트](SURVEY_DEPLOY.md) 기준.

| # | 항목 | 상태 |
|---|---|---|
| 1 | 파일럿 3건 소실 원인 규명 | ⛔ **차단 — 답변 대기** |
| 2 | 응답 수신 상태 확인 | ✅ 07-31 |
| 3 | 안내문·폼 제목 정정 | ✅ 07-31 (PASS 10 / FAIL 0) |
| 4 | 동탄 맘카페 매니저 사전 허락 | ☐ daria |
| 5 | 프리필 링크 실클릭 검증 | ✅ 07-31 (문15 자동 선택 확인) |
| 6 | 채널별 프리필 링크 사용 | ☐ 게시 시 |
| 7 | 문항 구조 동결 유지 | ☐ 게시 후 상시 |

응답은 **4건 그대로**(유효 2). 채널 유입 0.

## 다음 창에서 할 일 (우선순위 순)

1. **파일럿 답변 반영** → [SURVEY_DEPLOY §배포 이력](SURVEY_DEPLOY.md)에 기록, 체크리스트 1번 판정
2. **채널 본확산 게시** (daria 수동) — 맘카페 허락(4번)이 선행. 게시 후 채널·시각 기입
3. **응답 추이 관찰** → 마감 시: CSV → `survey_raw.csv` → `survey_encode.py` →
   `04_segmentation.py` → PERSONA_LADDER §5 승격/기각
4. **TRL 노출 수위 결정** (팀 결정 대기) — TRL은 정부 R&D 어휘. 심사자 배경에 따라
   노출 강도를 조절할지 ([MATURITY_POSITION §8](MATURITY_POSITION.md) 후속조치 1)
5. **장표 실물에 성숙도 라벨 반영** — 슬라이드·이미지는 저장소 밖이라 미점검.
   daria가 직접 (MATURITY_POSITION §8 후속조치 4)
6. **발표 서사 프레임 확정** (팀 결정 대기) — "산출 불가→감사점수" 전환을 "1회 사고"가
   아닌 "모델 특성→게이트 상시 작동"으로 서사화할지 (CX v4 결정 A)
7. 여유 시: GPT 이미지 2번 재생성분 검수(24° 부부 축·체크 제거·라운드 워치)

## 운영 메모

### 오늘 추가분

- **Apps Script 실행 시 함수 드롭다운을 반드시 확인할 것.** 기본값이 파일 첫 함수라
  그대로 누르면 `createSurveyForm`이 돌아 **사본 폼이 생긴다** — 07-31에 실제로 발생했다
  (즉시 폐기). 기존 사본 3개도 이 경로로 추정. 이제 가드가 막지만, 막힌 로그를 보고
  놀라지 말 것
- **폼 수정 계열**: `applyV3` / `applyV4` / `fixNoticeV5` — 폼 ID는
  [survey_form_V4.gs](survey_form_V4.gs) 상단 `LIVE_FORM_ID` 한 곳에만 있다
- **폼 검수 계열**: `verifyForm` / `verifyFormV4` / `verifyNoticeV5`.
  `verifyNoticeV5`는 안내문 정합뿐 아니라 **응답 수신 상태**까지 본다 (배포 직전 게이트)
- **편집기 코드는 저장소본이 진실이다.** 실행 전
  `cat docs/survey_form_V4.gs | clip`으로 통째 교체할 것 — 부분 붙여넣기는
  `LIVE_FORM_ID` 미정의로 전 함수가 터진다
- **응답 원문 보존 원칙**(daria 07-31): 설문 답변·인용은 가공하지 않고 원문 그대로,
  뉘앙스를 살려 간다. 비문·구어·오타까지 손대지 않는다 — Pain Point의 강도 자체가 근거다
- **옵시디언 미러는 잘 낡는다.** 원본과 대조 없이 미러를 신뢰하지 말 것.
  미러 파일은 헤더(frontmatter+`> [!note] 저장소 미러`) 아래에 원본 본문을 그대로 싣는
  구조라, 헤더만 보존하고 본문을 갈아끼우면 재생성된다

### 이전 창에서 이어짐

- **MoA 실행**: `python moa_orchestrator.py` — E 페르소나 랜덤 배정 로그 확인.
  강제: PowerShell에서 `$env:PERSONA_FORCE="Quietly Home"` (또는 "Night Keeper")
- **G 검수**: 신뢰도는 [G_AUDIT_SCORE.md](G_AUDIT_SCORE.md) v1.0 공식으로 직접 채점 —
  서브 출력에 신뢰도 수치가 보이면 그 자체가 규칙 위반(오염 의심 체크리스트행)
- **Docker DB**: 기동 중(onme_discovery_db, 포트 5433). 비밀번호는
  `%LOCALAPPDATA%\Temp\onme_db_pw.txt`(로컬 전용, 저장소 미기록). 원복은
  `docker compose -f db/compose.yml down -v`. Docker Desktop 재기동 실패 시
  deferred-work §Epic 5 부검 참조(재부팅+run 격리)
- 격리 폴더 3개(`Docker/run_stale*`·`docker-secrets-engine_stale`)는 삭제 불가
  방치 확정(무해)
- 동명 사본 폼 3개는 `[구버전-배포금지]`로 개명 격리 — 본 폼(`1x-JqmEH…`)만 유효

## 오늘 정정된 것 (숨기지 않는다)

운영 원칙 4에 따라 남긴다.

- **폼 제목 "3분→5분" 판단 오류(G)**: `applyV3`에 `setTitle('…5분 설문')`이 있어
  적용된 줄 알고 문서 제목을 고쳤으나, `verifyNoticeV5` 실측 결과 라이브 폼은
  여전히 `…3분 설문`이었다. 스크립트에 있다고 라이브에 반영된 것은 아니다 —
  **실측 없이 스크립트만 보고 상태를 단정하지 말 것.** 07-31 15:24 `fixNoticeV5`로
  실제 정정 완료
- **07-28 "본확산 완료" 오기**는 07-29에 이미 정정됨 (본확산 미시행)
